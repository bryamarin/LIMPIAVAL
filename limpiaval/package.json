{
  "name": "limpiaval-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Orquestador para el proyecto Limpiaval",
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend-react",
    "dev:api": "npm run dev --prefix backend",
    "dev:react": "npm run dev --prefix frontend-react",
    "dev": "concurrently \"npm:dev:api\" \"npm:dev:react\"",
    "db:setup": "cd backend && npx prisma migrate dev && npx prisma db seed && cd .."
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}